dist: trusty
sudo: required
language:
  - cpp
compiler:
  - gcc
addons:
  apt:
    packages:
      - libsfml-dev
script:
  - cmake -DCMAKE_BUILD_TYPE=Release -G "CodeBlocks - Unix Makefiles" ./
after_success:
  coveralls